//AzeTurk810
#include <iostream>
#include<cmath>
using namespace std;
int main()
{
    long long a,b,c,m;
    cin>>a>>b>>c>>m;
    if((a*b*c)%m==0)
    cout<<(a*b*c)/m;
    else
    cout<<(a*b*c)/m+1;
    return 0;
}
